import React from 'react';
import {StyleSheet, View, Text, TouchableOpacity} from 'react-native';

import globalStyle from '../styles/components/global.jsx';

function Button() {

    /*const onPressHandler = props.onPressHandler;
    const type = props.type;

    var source;
    var style;
    if (type == "delete"){
        source = require('../images/ReadIcon.png');
        style = globalStyle.readButton;
    } else if (type == "fav"){
        source = require('../images/FavIcon.png');
        style = globalStyle.readButton;
        style = globalStyle.readButton;
    } else if (type == "read"){
        source = require('../images/ReadIcon.png');
        style = globalStyle.readButton;

    return (
        <TouchableOpacity style={globalStyle.readButton} onPress={() => {onPressHandler()}}>
            <ImageBackground source={source} style={globalStyle.imageButton}/>
        </TouchableOpacity>
    )*/
}

export default Button;